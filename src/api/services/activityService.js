class activityService {
  // todo create model instance to use it here and save data.
  create(actvity) {

  }

  // update date bases on allowed ids or author ids

  // read data based on allowed users ids if its not public

  // delete only allowed by authors
  
}

module.exports = new activityService();